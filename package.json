{
  "name": "sedar-bot",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "SEDAR — proactive AI nutrition agent living in Telegram",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "trigger:dev": "trigger.dev dev",
    "trigger:deploy": "trigger.dev deploy"
  },
  "dependencies": {
    "@copilotkit/runtime": "latest",
    "@supabase/supabase-js": "latest",
    "@trigger.dev/sdk": "latest",
    "express": "^5.1.0",
    "express-oauth2-jwt-bearer": "latest",
    "openai": "latest"
  }
}
